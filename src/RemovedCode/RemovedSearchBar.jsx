{/* Search Bar */ }
{/* <div className="flex-1 max-w-md">
    <div className="relative">
        <input
            type="text"
            placeholder="Search..."
            value={searchQuery}
            onChange={(e) => setSearchQuery(e.target.value)}
            onFocus={() => setIsSearchFocused(true)}
            onBlur={() => setIsSearchFocused(false)}
            className={`w-full menu-panel text-gray-300 border ui-border-faint rounded-md py-2 px-4 pl-10 focus:outline-none transition-all ${isSearchFocused ? 'border-brand-primary/60 shadow-[0_0_0_2px_rgba(71,199,230,0.18)]' : 'hover:border-brand-primary/40'}`}
        />
        <div className="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
            <i className="fas fa-search text-gray-400"></i>
        </div>
    </div>
</div> */}